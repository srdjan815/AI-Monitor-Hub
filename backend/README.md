# AI Monitor Hub backend

The canonical project overview and setup guide live at:

- [`../README.md`](../README.md)
- [`../docs/development/local-setup.md`](../docs/development/local-setup.md)

The dependency source of truth is `backend/pyproject.toml`. Run backend tooling
from the repository root with `.venv`, or from `/app` inside the Docker API
container.
